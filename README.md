# Online Shop Backend

## Setup

1. Install dependencies:
   ```
   npm install
   ```

2. Start the server:
   ```
   npm start
   ```

3. Run tests:
   ```
   npm test
   ```

## API Endpoints

- `/customers`
- `/categories`
- `/items`
- `/orders`

Each supports standard CRUD operations.